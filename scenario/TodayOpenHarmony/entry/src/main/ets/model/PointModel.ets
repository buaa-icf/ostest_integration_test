export interface PointRule {
  title: string;
  points: string;
  desc: string;
}

export interface ExchangeOption {
  title: string;
  points: number;
  desc: string;
}

export interface LevelRule {
  level: string;
  points: string;
  benefits: string;
}
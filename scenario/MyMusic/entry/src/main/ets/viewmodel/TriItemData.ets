import MediaItemData from './MediaItemData';
import MediaItemInit from './MediaItemInit';

export default class TriItemData extends MediaItemData {
  constructor(init: MediaItemInit) {
    super(init);
  }
}
